tity: plan.implementation.workflow, materialWorkerProvider: plan.implementation.provider });"
  );
  s = replaceOnce(
    s,
    "          source_workflow_path: targetPolicy.ciWorkflowPath,\n          implementation_attempt: String(state.implementationAttempts)\n        }",
    "          source_workflow_path: targetPolicy.ciWorkflowPath,\n          implementation_attempt: String(state.implementationAttempts),\n          implementer_provider: plan.implementation.provider,\n          implementer_worker_identity: plan.implementation.workflow,\n          implementer_run_id: String(worker.id)\n        }",
    'initial controller audit lineage inputs'
  );
  s = replaceOnce(
    s,
    "      const releaseInput = {\n        schemaVersion: 1,\n        repository: targetRepository,\n        pullRequestNumber: pullRequest.number,\n        materialHeadSha,\n        currentRemoteHeadSha: String((await fetchPullRequest(targetRepository, pullRequest.number, targetReadToken)).head.sha).toLowerCase(),",
    "      const finalPullRequest = await fetchPullRequest(targetRepository, pullRequest.number, targetReadToken);\n      const releaseIdentity = releaseIdentityFromPullRequest(finalPullRequest, { materialHeadSha, baseSha: expectedBaseSha });\n      const releaseInput = {\n        schemaVersion: 1,\n        repository: targetRepository,\n        pullRequestNumber: pullRequest.number,\n        materialHeadSha,\n        currentRemoteHeadSha: releaseIdentity.currentRemoteHeadSha,",
    'initial controller final identity refresh'
  );
  s = replaceOnce(s, '        mergePreview: null,', '        mergePreview: releaseIdentity.mergePreview,', 'initial controller merge preview gate');
  await save(path, s);
}

// 7) Resume controller: reconcile base drift, preserve lineage, bind audit, and evaluate fresh release identity.
{
  const path = 'scripts/resume-delivery-v2-controller.mjs';
  let s = await load(path);
  s = replaceOnce(
    s,
    "import { ciFailureClassForConclusion, createDispatchNonce, loadAuthoritativeAuditResult, publishReleaseStatus, selectCorrelatedWorkflowRun } from '../src/v2/controller-runtime.mjs';",
    "import { ciFailureClassForConclusion, createDispatchNonce, loadAuthoritativeAuditResult, publishReleaseStatus, releaseIdentityFromPullRequest, selectCorrelatedWorkflowRun } from '../src/v2/controller-runtime.mjs';",
    'resume controller shared release helper import'
  );
  s = replaceOnce(
    s,
    "  let materialHeadSha = String(pullRequest.head.sha).toLowerCase();\n  let changedPaths = await fetchChangedPaths",
    "  let materialHeadSha = String(pullRequest.head.sha).toLowerCase();\n  const expectedBaseSha = String(pullRequest.base.sha).toLowerCase();\n  let changedPaths = await fetchChangedPaths",
    'resume controller expected base sha'
  );
  s = replaceOnce(
    s,
    "      headRef: String(pullRequest.head.ref),\n      remoteHeadSha: materialHeadSha\n    });",
    "      headRef: String(pullRequest.head.ref),\n      baseSha: String(pullRequest.base.sha),\n      remoteHeadSha: materialHeadSha\n    });",
    'resume persistent base reconciliation'
  );
  s = s.replaceAll(
    "await persist({ nextAction: 'observe-ci', workerRunId: worker.id });",
    "await persist({ nextAction: 'observe-ci', workerRunId: worker.id, materialWorkerRunId: worker.id, materialWorkerIdentity: plan.implementation.workflow, materialWorkerProvider: plan.implementation.provider });"
  );
  s = replaceOnce(
    s,
    "            source_workflow_path: targetPolicy.ciWorkflowPath, implementation_attempt: String(state.implementationAttempts)\n          }",
    "            source_workflow_path: targetPolicy.ciWorkflowPath, implementation_attempt: String(state.implementationAttempts),\n            implementer_provider: controller.materialWorkerProvider ?? provider,\n            implementer_worker_identity: controller.materialWorkerIdentity ?? plan.implementation.workflow,\n            implementer_run_id: String(controller.materialWorkerRunId ?? controller.workerRunId)\n          }",
    'resume controller audit lineage inputs'
  );
  s = replaceOnce(
    s,
    "    const releaseInput = {\n      schemaVersion: 1, repository: targetRepository, pullRequestNumber: resumePr, materialHeadSha, currentRemoteHeadSha: materialHeadSha,",
    "    const finalPullRequest = await fetchPullRequest(targetRepository, resumePr, targetReadToken);\n    const releaseIdentity = releaseIdentityFromPullRequest(finalPullRequest, { materialHeadSha, baseSha: expectedBaseSha });\n    const releaseInput = {\n      schemaVersion: 1, repository: targetRepository, pullRequestNumber: resumePr, materialHeadSha, currentRemoteHeadSha: releaseIdentity.currentRemoteHeadSha,",
    'resume controller final identity refresh'
  );
  s = replaceOnce(s, '      mergePreview: null,', '      mergePreview: releaseIdentity.mergePreview,', 'resume controller merge preview gate');
  await save(path, s);
}

// 8) Focused regression tests for the three audit escapes.
{
  con
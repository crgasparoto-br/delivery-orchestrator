## Summary

- makes `CODEX_AUTH_MODE=chatgpt` the default while keeping explicit `api-key` fallback
- uses persistent, separate `CODEX_HOME` directories for implementer and auditor without deleting `auth.json`
- strips API/access-token auth variables in ChatGPT mode and forces the Codex login method
- moves the delivery workflow to a serialized dedicated self-hosted Linux runner
- fails closed for ChatGPT-managed auth while the control repository is public
- preserves the exact-head CI wait added on `main`
- keeps the auditor signing key outside persistent Codex homes and removes runtime directories before artifact upload
- isolates child-process credentials by role: implementer receives only the selected write token; auditor receives only the selected read token plus explicit audit-only inputs
- documents one-time device-auth bootstrap and security constraints

## Validation

- `node --test test/*.test.mjs`: 20/20 passing
- `node src/cli.mjs validate`: passing
- role-boundary adversarial controls passed for implementer, auditor, `extraEnv` reinjection, and unknown roles
- material head: `3dfa0a79a186be2522cdd4079cbffff1e9b93146`
- certified result-only handoff head: `b21d215364397a56f8f8d01c8a1c860b97ccc8af`
- result-only handoff guard: passing
- remote PR diff: 2 commits, 21 changed paths total (9 material + 12 `.audit/entregar-issue/*` handoff paths)
- current exact-head GitHub Actions observation: no workflow runs associated with the published head

## Operational status

- repository visibility is private
- dedicated self-hosted runner is provisioned with the `delivery-orchestrator` label
- implementer and auditor ChatGPT/Codex authentication is provisioned and validated
- repository variables for ChatGPT mode are configured
- existing GitHub write/read delivery credentials and auditor signing secrets will be reused
- `OPENAI_API_KEY` may remain configured as an explicit `api-key` fallback; ChatGPT mode strips API/access-token credentials from the Codex child environment
- handoff certificate contract: `2026-08-06.2`
- previous independent finding `A-001` is remediated and inherited controls are published for re-audit

No merge is performed by this change. A new independent audit is still required for release approval.

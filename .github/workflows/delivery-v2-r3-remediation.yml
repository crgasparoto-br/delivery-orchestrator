name: Delivery V2 R3 Remediation

on:
  push:
    branches:
      - feat/63-v2-operational-efficiency
    paths:
      - '.github/workflows/delivery-v2-r3-remediation.yml'

permissions:
  contents: write

jobs:
  remediate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/63-v2-operational-efficiency
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci --no-audit --no-fund
      - name: Apply systemic remediation
        shell: bash
        run: |
          node <<'NODE'
          const fs = require('node:fs');

          function read(path) { return fs.readFileSync(path, 'utf8'); }
          function write(path, value) { fs.writeFileSync(path, value); }
          function replace(path, oldText, newText) {
            const before = read(path);
            if (!before.includes(oldText)) throw new Error(`missing patch anchor in ${path}: ${oldText.slice(0, 100)}`);
            write(path, before.replace(oldText, newText));
          }
          function replaceRegex(path, regex, replacement) {
            const before = read(path);
            const after = before.replace(regex, replacement);
            if (after === before) throw new Error(`regex patch did not match ${path}: ${regex}`);
            write(path, after);
          }

          write('src/v2/controller-provenance.mjs', `const TRUSTED_ASSOCIATIONS = new Set(['OWNER', 'MEMBER', 'COLLABORATOR']);

function requiredString(value, label) {
  const result = String(value ?? '').trim();
  if (!result) throw new Error(label + ' is required');
  return result;
}

function requiredPositiveInteger(value, label) {
  const result = Number(value);
  if (!Number.isInteger(result) || result < 1) throw new Error(label + ' must be a positive integer');
  return result;
}

export function trustedCommentAuthorForRepository(repository) {
  const value = requiredString(repository, 'repository');
  const owner = value.split('/')[0];
  if (!owner) throw new Error('repository must use owner/name form');
  return owner.toLowerCase();
}

export function selectTrustedMarkerComment(comments, { marker, label, trustedLogin } = {}) {
  if (!Array.isArray(comments)) throw new Error('comments must be an array');
  const expectedMarker = requiredString(marker, 'marker');
  const expectedLabel = requiredString(label, 'label');
  const expectedLogin = requiredString(trustedLogin, 'trustedLogin').toLowerCase();
  const matching = comments.filter((comment) => String(comment?.body ?? '').startsWith(expectedMarker));
  const untrusted = matching.filter((comment) => {
    const login = String(comment?.user?.login ?? '').toLowerCase();
    const association = String(comment?.author_association ?? '').toUpperCase();
    return login !== expectedLogin || (association && !TRUSTED_ASSOCIATIONS.has(association));
  });
  if (untrusted.length > 0) throw new Error('untrusted ' + expectedLabel + ' marker comment found; refusing controller state recovery');
  if (matching.length === 0) return null;
  if (matching.length > 1) throw new Error('multiple ' + expectedLabel + ' comments found; refusing ambiguous recovery');
  return matching[0];
}

export function parseTrustedJsonEnvelope(comments, { marker, label, trustedLogin } = {}) {
  const comment = selectTrustedMarkerComment(comments, { marker, label, trustedLogin });
  if (!comment) return null;
  const body = String(comment.body ?? '');
  const fenced = body.match(/\x60\x60\x60json\\s*([\\s\\S]*?)\\s*\x60\x60\x60/);
  if (!fenced) throw new Error(String(label) + ' comment is missing its JSON envelope');
  return Object.freeze({ commentId: comment.id ?? null, userLogin: String(comment.user?.login ?? ''), value: JSON.parse(fenced[1]) });
}

export function validateControllerRunProvenance(run, { orchestratorRepository, trustedRef, workflowPath = '.github/workflows/delivery-v2-dispatch.yml' } = {}) {
  if (!run || Array.isArray(run) || typeof run !== 'object') throw new Error('controller workflow run is required');
  requiredPositiveInteger(run.id, 'controller workflow run id');
  const repository = requiredString(orchestratorRepository, 'orchestratorRepository');
  if (String(run.repository?.full_name ?? '') !== repository) throw new Error('controller workflow run repository mismatch');
  if (String(run.event ?? '') !== 'workflow_dispatch') throw new Error('controller workflow run event mismatch');
  if (String(run.path ?? '') !== requiredString(workflowPath, 'workflowPath')) throw new Error('controller workflow path mismatch');
  if (String(run.head_branch ?? '') !== requiredString(trustedRef, 'trustedRef')) throw new Error('controller workflow trusted ref mismatch');
  return Object.freeze({ runId: Number(run.id), repository, workflowPath: String(run.path), trustedRef: String(run.head_branch) });
}
`);

          // Preserve full semantic audit findings in the state machine.
          replace('src/v2/remediation-state-machine.mjs',
`      candidateSha: requiredSha(finding.candidateSha, \`findings[\${index}].candidateSha\`),
      blocksRelease: finding.blocksRelease === true,
      remediationMode: requiredString(finding.remediationMode, \`findings[\${index}].remediationMode\`),
      surface: requiredString(finding.surface, \`findings[\${index}].surface\`),
      failureMode: requiredString(finding.failureMode, \`findings[\${index}].failureMode\`)
`,
`      candidateSha: requiredSha(finding.candidateSha, \`findings[\${index}].candidateSha\`),
      severity: requiredString(finding.severity, \`findings[\${index}].severity\`),
      violatedContract: requiredString(finding.violatedContract, \`findings[\${index}].violatedContract\`),
      blocksRelease: finding.blocksRelease === true,
      remediationMode: requiredString(finding.remediationMode, \`findings[\${index}].remediationMode\`),
      surface: requiredString(finding.surface, \`findings[\${index}].surface\`),
      failureMode: requiredString(finding.failureMode, \`findings[\${index}].failureMode\`),
      evidence: requiredString(finding.evidence, \`findings[\${index}].evidence\`)
`);

          // Persistent state keeps complete remediation packets.
          replace('src/v2/persistent-state.mjs',
`const STATE_SET = new Set(DELIVERY_V2_STATES);
`,
`const STATE_SET = new Set(DELIVERY_V2_STATES);
const CI_FAILURE_CLASSES = new Set(['actionable', 'external', 'preexisting']);
const AUDIT_DECISIONS = new Set(['approved', 'rejected']);
`);
          replaceRegex('src/v2/persistent-state.mjs', /function normalizeFinding\(finding, index\) \{[\s\S]*?\n\}\n\nfunction normalizeEvidenceRefs/, `function normalizeFinding(finding, index) {
  const value = requireObject(finding, \`blockingFindings[\${index}]\`);
  const remediationMode = requireString(value.remediationMode, \`blockingFindings[\${index}].remediationMode\`).toLowerCase();
  if (!['targeted', 'systemic'].includes(remediationMode)) throw new Error(\`blockingFindings[\${index}].remediationMode must be targeted or systemic\`);
  return Object.freeze({
    id: requireString(value.id, \`blockingFindings[\${index}].id\`),
    candidateSha: requireSha(value.candidateSha, \`blockingFindings[\${index}].candidateSha\`),
    severity: requireString(value.severity, \`blockingFindings[\${index}].severity\`).toLowerCase(),
    violatedContract: requireString(value.violatedContract, \`blockingFindings[\${index}].violatedContract\`),
    surface: requireString(value.surface, \`blockingFindings[\${index}].surface\`),
    failureMode: requireString(value.failureMode, \`blockingFindings[\${index}].failureMode\`),
    evidence: requireString(value.evidence, \`blockingFindings[\${index}].evidence\`),
    remediationMode,
    blocksRelease: value.blocksRelease === true,
    evidenceRef: requireString(value.evidenceRef, \`blockingFindings[\${index}].evidenceRef\`)
  });
}

function normalizeCiFailure(value, materialHeadSha) {
  if (value == null) return null;
  const failure = requireObject(value, 'ciFailure');
  const candidateSha = requireSha(failure.candidateSha, 'ciFailure.candidateSha');
  if (candidateSha !== materialHeadSha) throw new Error('ciFailure is stale for material head');
  const failureClass = requireString(failure.failureClass, 'ciFailure.failureClass').toLowerCase();
  if (!CI_FAILURE_CLASSES.has(failureClass)) throw new Error('ciFailure.failureClass is unsupported');
  return Object.freeze({
    candidateSha,
    failureClass,
    cause: requireString(failure.cause, 'ciFailure.cause'),
    evidenceRef: requireString(failure.evidenceRef, 'ciFailure.evidenceRef')
  });
}

function normalizeAuditEvidence(value, materialHeadSha) {
  if (value == null) return null;
  const evidence = requireObject(value, 'auditEvidence');
  const candidateSha = requireSha(evidence.candidateSha, 'auditEvidence.candidateSha');
  if (candidateSha !== materialHeadSha) throw new Error('auditEvidence is stale for material head');
  const decision = requireString(evidence.decision, 'auditEvidence.decision').toLowerCase();
  if (!AUDIT_DECISIONS.has(decision)) throw new Error('auditEvidence.decision is unsupported');
  return Object.freeze({ candidateSha, decision, evidenceRef: requireString(evidence.evidenceRef, 'auditEvidence.evidenceRef') });
}

function normalizeEvidenceRefs`);
          replace('src/v2/persistent-state.mjs',
`  const workflowChecks = (value.workflowChecks ?? []).map(normalizeCheck);
  const blockingFindings = (value.blockingFindings ?? []).map(normalizeFinding);

  return Object.freeze({
`,
`  const workflowChecks = (value.workflowChecks ?? []).map(normalizeCheck);
  const blockingFindings = (value.blockingFindings ?? []).map(normalizeFinding);
  const ciFailure = normalizeCiFailure(value.ciFailure, materialHeadSha);
  const auditEvidence = normalizeAuditEvidence(value.auditEvidence, materialHeadSha);
  const status = normalizeStatus(value.status);
  if (status === 'ci-failed-remediable' && (!ciFailure || ciFailure.failureClass !== 'actionable')) throw new Error('ci-failed-remediable requires a persisted actionable ciFailure');
  if (status === 'audit-failed-remediable' && blockingFindings.filter((finding) => finding.blocksRelease).length === 0) throw new Error('audit-failed-remediable requires persisted blocking findings');

  return Object.freeze({
`);
          replace('src/v2/persistent-state.mjs', `    status: normalizeStatus(value.status),`, `    status,`);
          replace('src/v2/persistent-state.mjs',
`    workflowChecks: Object.freeze(workflowChecks),
    blockingFindings: Object.freeze(blockingFindings),
`,
`    workflowChecks: Object.freeze(workflowChecks),
    ciFailure,
    auditEvidence,
    blockingFindings: Object.freeze(blockingFindings),
`);
          replace('src/v2/persistent-state.mjs',
`    workflowChecks: value.workflowChecks ?? [],
    blockingFindings: value.blockingFindings ?? [],
`,
`    workflowChecks: value.workflowChecks ?? [],
    ciFailure: value.ciFailure ?? null,
    auditEvidence: value.auditEvidence ?? null,
    blockingFindings: value.blockingFindings ?? [],
`);
          replace('src/v2/persistent-state.mjs',
`    workflowChecks: [],
    blockingFindings: [],
    evidenceRefs: [],
`,
`    workflowChecks: [],
    ciFailure: null,
    auditEvidence: null,
    blockingFindings: [],
    evidenceRefs: [],
`);
          replace('src/v2/persistent-state.mjs',
`  const workflowChecks = value.workflowChecks == null ? state.workflowChecks : value.workflowChecks.map(normalizeCheck);
  const blockingFindings = value.blockingFindings == null ? state.blockingFindings : value.blockingFindings.map(normalizeFinding);
  assertEvidenceBoundToHead(workflowChecks, blockingFindings, state.materialHeadSha);
`,
`  const workflowChecks = value.workflowChecks == null ? state.workflowChecks : value.workflowChecks.map(normalizeCheck);
  const ciFailure = value.ciFailure === undefined ? state.ciFailure : normalizeCiFailure(value.ciFailure, state.materialHeadSha);
  const auditEvidence = value.auditEvidence === undefined ? state.auditEvidence : normalizeAuditEvidence(value.auditEvidence, state.materialHeadSha);
  const blockingFindings = value.blockingFindings == null ? state.blockingFindings : value.blockingFindings.map(normalizeFinding);
  assertEvidenceBoundToHead(workflowChecks, blockingFindings, state.materialHeadSha);
`);
          replace('src/v2/persistent-state.mjs',
`    workflowChecks,
    blockingFindings,
    evidenceRefs: value.evidenceRefs == null ? state.evidenceRefs : normalizeEvidenceRefs(value.evidenceRefs),
`,
`    workflowChecks,
    ciFailure,
    auditEvidence,
    blockingFindings,
    evidenceRefs: value.evidenceRefs == null ? state.evidenceRefs : normalizeEvidenceRefs(value.evidenceRefs),
`);

          // Operational projection/restoration is lossless for authorized remediation data.
          replace('src/v2/operational-controller.mjs', `  const findingEvidenceRef = current.auditEvidence?.evidenceRef ?? 'delivery-v2:operational-state';\n`, '');
          replace('src/v2/operational-controller.mjs',
`    workflowChecks,
    blockingFindings: (current.blockingFindings ?? []).map((finding) => ({
      id: finding.id,
      candidateSha: finding.candidateSha,
      surface: finding.surface,
      failureMode: finding.failureMode,
      evidenceRef: findingEvidenceRef
    })),
`,
`    workflowChecks,
    ciFailure: current.ciFailure,
    auditEvidence: current.auditEvidence,
    blockingFindings: (current.blockingFindings ?? []).map((finding) => ({
      id: finding.id,
      candidateSha: finding.candidateSha,
      severity: finding.severity,
      violatedContract: finding.violatedContract,
      surface: finding.surface,
      failureMode: finding.failureMode,
      evidence: finding.evidence,
      remediationMode: finding.remediationMode,
      blocksRelease: finding.blocksRelease,
      evidenceRef: current.auditEvidence?.evidenceRef ?? 'delivery-v2:operational-state'
    })),
`);
          replace('src/v2/operational-controller.mjs',
`    auditEvidence: null,
    ciFailure: null,
    blockingFindings: Object.freeze(persistent.blockingFindings.map((finding) => Object.freeze({
      id: finding.id,
      candidateSha: finding.candidateSha,
      blocksRelease: true,
      remediationMode: 'targeted',
      surface: finding.surface,
      failureMode: finding.failureMode
    }))),
`,
`    auditEvidence: persistent.auditEvidence,
    ciFailure: persistent.ciFailure,
    blockingFindings: Object.freeze(persistent.blockingFindings.map((finding) => Object.freeze({
      id: finding.id,
      candidateSha: finding.candidateSha,
      severity: finding.severity,
      violatedContract: finding.violatedContract,
      blocksRelease: finding.blocksRelease,
      remediationMode: finding.remediationMode,
      surface: finding.surface,
      failureMode: finding.failureMode,
      evidence: finding.evidence
    }))),
`);

          // Merge preview must be backed by an observed successful workflow job, never PR mergeability alone.
          replace('src/v2/controller-runtime.mjs',
`  const mergeable = pullRequest.mergeable;
  const terminal = previewSha != null && mergeable != null;
  const evidenceRef = requiredString(pullRequest.html_url ?? pullRequest.url ?? ('github:pull:' + (pullRequest.number ?? 'unknown')), 'pullRequest evidence URL');
  return Object.freeze({
    currentRemoteHeadSha: currentHead,
    currentBaseSha: currentBase,
    mergePreview: Object.freeze({
      required: true,
      materialHeadSha: expectedHead,
      previewSha,
      status: terminal ? 'completed' : 'pending',
      conclusion: terminal ? (mergeable === true ? 'success' : 'failure') : null,
      evidenceRef
    })
  });
}
`,
`  const evidenceRef = requiredString(pullRequest.html_url ?? pullRequest.url ?? ('github:pull:' + (pullRequest.number ?? 'unknown')), 'pullRequest evidence URL');
  return Object.freeze({
    currentRemoteHeadSha: currentHead,
    currentBaseSha: currentBase,
    mergePreview: Object.freeze({
      required: true,
      materialHeadSha: expectedHead,
      previewSha,
      status: 'pending',
      conclusion: null,
      evidenceRef
    })
  });
}

export function mergePreviewEvidenceFromWorkflow({ pullRequest, materialHeadSha, baseSha, workflowRun, jobs = [], requiredJobName } = {}) {
  const identity = releaseIdentityFromPullRequest(pullRequest, { materialHeadSha, baseSha });
  if (!identity.mergePreview.previewSha) return identity.mergePreview;
  if (!workflowRun || typeof workflowRun !== 'object' || Array.isArray(workflowRun)) throw new Error('merge-preview source workflow run is required');
  const expectedHead = requiredSha(materialHeadSha, 'materialHeadSha');
  const expectedBase = requiredSha(baseSha, 'baseSha');
  if (String(workflowRun.event ?? '') !== 'pull_request') throw new Error('merge-preview evidence must come from a pull_request workflow run');
  if (requiredSha(workflowRun.head_sha, 'workflowRun.head_sha') !== expectedHead) throw new Error('merge-preview workflow run head mismatch');
  const prNumber = requiredPositiveInteger(pullRequest.number, 'pullRequest.number');
  const runPr = (workflowRun.pull_requests ?? []).find((item) => Number(item?.number) === prNumber);
  if (!runPr) throw new Error('merge-preview workflow run is not bound to the target PR');
  if (requiredSha(runPr.head?.sha, 'workflowRun.pullRequest.head.sha') !== expectedHead) throw new Error('merge-preview workflow PR head mismatch');
  if (requiredSha(runPr.base?.sha, 'workflowRun.pullRequest.base.sha') !== expectedBase) throw new Error('merge-preview workflow PR base mismatch');
  if (!Array.isArray(jobs)) throw new Error('merge-preview jobs must be an array');
  const jobName = requiredString(requiredJobName, 'requiredJobName');
  const matches = jobs.filter((job) => String(job?.name ?? '') === jobName);
  if (matches.length > 1) throw new Error('ambiguous merge-preview validation jobs');
  const job = matches[0];
  if (!job) return identity.mergePreview;
  return Object.freeze({
    ...identity.mergePreview,
    status: String(job.status ?? '').toLowerCase() || 'pending',
    conclusion: job.conclusion == null ? null : String(job.conclusion).toLowerCase(),
    evidenceRef: requiredString(job.html_url ?? workflowRun.html_url, 'merge-preview job evidence URL')
  });
}

export async function collectMergePreviewEvidence({ repository, pullRequest, materialHeadSha, baseSha, workflowRun, requiredJobName, token } = {}) {
  const repo = requiredString(repository, 'repository');
  const runId = requiredPositiveInteger(workflowRun?.id, 'workflowRun.id');
  const payload = await fetchJson(\`https://api.github.com/repos/\${repo}/actions/runs/\${runId}/jobs?per_page=100\`, token);
  return mergePreviewEvidenceFromWorkflow({ pullRequest, materialHeadSha, baseSha, workflowRun, jobs: payload.jobs ?? [], requiredJobName });
}
`);

          // Trusted comment provenance and controller workflow lineage.
          replace('scripts/guard-delivery-v2-reentry.mjs',
`import { expectedDispatchTitle, selectCorrelatedWorkflowRun } from '../src/v2/controller-runtime.mjs';
`,
`import { expectedDispatchTitle, selectCorrelatedWorkflowRun } from '../src/v2/controller-runtime.mjs';
import { parseTrustedJsonEnvelope, trustedCommentAuthorForRepository, validateControllerRunProvenance } from '../src/v2/controller-provenance.mjs';
`);
          replaceRegex('scripts/guard-delivery-v2-reentry.mjs', /function parseJsonEnvelope\(comments, marker, label\) \{[\s\S]*?\n\}\n\nexport function parsePersistentStateEnvelope\(comments\) \{/, `function parseJsonEnvelope(comments, marker, label, trustedLogin) {
  return parseTrustedJsonEnvelope(comments, { marker, label, trustedLogin });
}

export function parsePersistentStateEnvelope(comments, { trustedLogin } = {}) {`);
          replace('scripts/guard-delivery-v2-reentry.mjs', `  const parsed = parseJsonEnvelope(comments, STATE_MARKER, 'Delivery V2 state');`, `  const parsed = parseJsonEnvelope(comments, STATE_MARKER, 'Delivery V2 state', trustedLogin);`);
          replace('scripts/guard-delivery-v2-reentry.mjs', `export function parseBootstrapLease(comments) {\n  const parsed = parseJsonEnvelope(comments, BOOTSTRAP_MARKER, 'Delivery V2 bootstrap state');`, `export function parseBootstrapLease(comments, { trustedLogin } = {}) {\n  const parsed = parseJsonEnvelope(comments, BOOTSTRAP_MARKER, 'Delivery V2 bootstrap state', trustedLogin);`);
          replace('scripts/guard-delivery-v2-reentry.mjs',
`  const candidates = pulls.filter((pr) =>
    String(pr?.base?.ref ?? '') === base
    && String(pr?.title ?? '').startsWith('[delivery-v2] ')
    && closing.test(String(pr?.body ?? ''))
  );
`,
`  const trustedLogin = arguments[1]?.trustedLogin == null ? null : String(arguments[1].trustedLogin).toLowerCase();
  const candidates = pulls.filter((pr) =>
    String(pr?.base?.ref ?? '') === base
    && String(pr?.title ?? '').startsWith('[delivery-v2] ')
    && closing.test(String(pr?.body ?? ''))
    && (!trustedLogin || String(pr?.user?.login ?? '').toLowerCase() === trustedLogin)
  );
`);
          replace('scripts/guard-delivery-v2-reentry.mjs',
`  const pulls = await listOpenPullRequests(targetRepository, baseBranch, readToken);
  const pullRequest = selectManagedPullRequest(pulls, { issueNumber, baseBranch });
  let stateEnvelope = null;
  let bootstrapLease = null;
  if (pullRequest) stateEnvelope = parsePersistentStateEnvelope(await listIssueComments(targetRepository, pullRequest.number, readToken));
  else bootstrapLease = parseBootstrapLease(await listIssueComments(targetRepository, issueNumber, readToken));

  const recoveredWorkerRun = bootstrapLease ? await recoverBootstrapWorkerRun(bootstrapLease, orchestratorRepository, orchestratorRef, actionsToken) : null;
`,
`  const trustedLogin = trustedCommentAuthorForRepository(targetRepository);
  const pulls = await listOpenPullRequests(targetRepository, baseBranch, readToken);
  const pullRequest = selectManagedPullRequest(pulls, { issueNumber, baseBranch, trustedLogin });
  let stateEnvelope = null;
  let bootstrapLease = null;
  if (pullRequest) stateEnvelope = parsePersistentStateEnvelope(await listIssueComments(targetRepository, pullRequest.number, readToken), { trustedLogin });
  else bootstrapLease = parseBootstrapLease(await listIssueComments(targetRepository, issueNumber, readToken), { trustedLogin });

  const provenance = stateEnvelope?.controller ?? bootstrapLease;
  if (provenance) {
    const controllerRunId = positiveInteger(provenance.controllerRunId, 'controllerRunId');
    const controllerRun = await api(\`https://api.github.com/repos/\${orchestratorRepository}/actions/runs/\${controllerRunId}\`, actionsToken);
    validateControllerRunProvenance(controllerRun, { orchestratorRepository, trustedRef: orchestratorRef });
  }
  const recoveredWorkerRun = bootstrapLease ? await recoverBootstrapWorkerRun(bootstrapLease, orchestratorRepository, orchestratorRef, actionsToken) : null;
`);

          // Reservation writer refuses untrusted marker reuse.
          replace('scripts/reserve-delivery-v2-initial-attempt.mjs', `import { createDispatchNonce } from '../src/v2/controller-runtime.mjs';\n`, `import { createDispatchNonce } from '../src/v2/controller-runtime.mjs';\nimport { selectTrustedMarkerComment, trustedCommentAuthorForRepository } from '../src/v2/controller-provenance.mjs';\n`);
          replace('scripts/reserve-delivery-v2-initial-attempt.mjs',
`    const comments = await api(\`https://api.github.com/repos/\${repository}/issues/\${issueNumber}/comments?per_page=100\`, readToken);
    const existing = comments.find((comment) => String(comment.body ?? '').startsWith(BOOTSTRAP_MARKER));
`,
`    const comments = await api(\`https://api.github.com/repos/\${repository}/issues/\${issueNumber}/comments?per_page=100\`, readToken);
    const existing = selectTrustedMarkerComment(comments, { marker: BOOTSTRAP_MARKER, label: 'Delivery V2 bootstrap state', trustedLogin: trustedCommentAuthorForRepository(repository) });
`);

          // Normal controller uses trusted state-comment writer and observed merge-preview workflow evidence.
          replace('scripts/run-delivery-v2-controller.mjs',
`import { ciFailureClassForEvidence, collectCiFailureEvidence, createDispatchNonce, loadAuthoritativeAuditResult, publishReleaseStatus, releaseIdentityFromPullRequest, selectCorrelatedWorkflowRun } from '../src/v2/controller-runtime.mjs';
`,
`import { ciFailureClassForEvidence, collectCiFailureEvidence, collectMergePreviewEvidence, createDispatchNonce, loadAuthoritativeAuditResult, publishReleaseStatus, releaseIdentityFromPullRequest, selectCorrelatedWorkflowRun } from '../src/v2/controller-runtime.mjs';
import { selectTrustedMarkerComment, trustedCommentAuthorForRepository } from '../src/v2/controller-provenance.mjs';
`);
          replace('scripts/run-delivery-v2-controller.mjs',
`  const existing = comments.find((comment) => String(comment.body ?? '').startsWith(STATE_MARKER));
`,
`  const existing = selectTrustedMarkerComment(comments, { marker: STATE_MARKER, label: 'Delivery V2 state', trustedLogin: trustedCommentAuthorForRepository(repository) });
`);
          replace('scripts/run-delivery-v2-controller.mjs',
`    const candidates = pulls.filter((pr) => Date.parse(pr.created_at) >= since - 5000 && String(pr.title ?? '').startsWith('[delivery-v2] ') && closing.test(String(pr.body ?? '')));
`,
`    const trustedLogin = trustedCommentAuthorForRepository(repository);
    const candidates = pulls.filter((pr) => Date.parse(pr.created_at) >= since - 5000 && String(pr.title ?? '').startsWith('[delivery-v2] ') && closing.test(String(pr.body ?? '')) && String(pr.user?.login ?? '').toLowerCase() === trustedLogin && String(pr.head?.repo?.full_name ?? repository) === repository);
`);
          replace('scripts/run-delivery-v2-controller.mjs',
`  const actionsToken = requiredEnv('GITHUB_TOKEN');
`,
`  const actionsToken = requiredEnv('GITHUB_TOKEN');
  const controllerRunId = positiveInteger(requiredEnv('GITHUB_RUN_ID'), 'GITHUB_RUN_ID');
`);
          replace('scripts/run-delivery-v2-controller.mjs', `  let controller = {};`, `  let controller = { controllerRunId, controllerRepository: orchestratorRepository, controllerRef: orchestratorRef, controllerWorkflowPath: '.github/workflows/delivery-v2-dispatch.yml' };`);
          replace('scripts/run-delivery-v2-controller.mjs',
`      const releaseIdentity = releaseIdentityFromPullRequest(finalPullRequest, { materialHeadSha, baseSha: expectedBaseSha });
      const releaseInput = {
`,
`      const releaseIdentity = releaseIdentityFromPullRequest(finalPullRequest, { materialHeadSha, baseSha: expectedBaseSha });
      const mergePreview = await collectMergePreviewEvidence({ repository: targetRepository, pullRequest: finalPullRequest, materialHeadSha, baseSha: expectedBaseSha, workflowRun: latestSourceRun, requiredJobName: targetPolicy.mergePreviewJobName, token: targetReadToken });
      const releaseInput = {
`);
          replace('scripts/run-delivery-v2-controller.mjs', `        mergePreview: releaseIdentity.mergePreview,`, `        mergePreview,`);

          // Resume controller validates persisted comment + workflow provenance and preserves current controller lineage.
          replace('scripts/resume-delivery-v2-controller.mjs',
`import { ciFailureClassForEvidence, collectCiFailureEvidence, createDispatchNonce, loadAuthoritativeAuditResult, publishReleaseStatus, releaseIdentityFromPullRequest, selectCorrelatedWorkflowRun } from '../src/v2/controller-runtime.mjs';
`,
`import { ciFailureClassForEvidence, collectCiFailureEvidence, collectMergePreviewEvidence, createDispatchNonce, loadAuthoritativeAuditResult, publishReleaseStatus, releaseIdentityFromPullRequest, selectCorrelatedWorkflowRun } from '../src/v2/controller-runtime.mjs';
import { parseTrustedJsonEnvelope, selectTrustedMarkerComment, trustedCommentAuthorForRepository, validateControllerRunProvenance } from '../src/v2/controller-provenance.mjs';
`);
          replaceRegex('scripts/resume-delivery-v2-controller.mjs', /function parseStateComment\(comments\) \{[\s\S]*?\n\}\n\nasync function classifierIdentity/, `function parseStateComment(comments, trustedLogin) {
  const parsed = parseTrustedJsonEnvelope(comments, { marker: STATE_MARKER, label: 'Delivery V2 state', trustedLogin });
  if (!parsed) return null;
  const envelope = parsed.value;
  if (!envelope?.persistent) throw new Error('Delivery V2 state comment is missing persistent state');
  return Object.freeze({ commentId: parsed.commentId, persistent: envelope.persistent, controller: envelope.controller ?? {} });
}

async function classifierIdentity`);
          replace('scripts/resume-delivery-v2-controller.mjs',
`  const existing = comments.find((comment) => String(comment.body ?? '').startsWith(STATE_MARKER));
`,
`  const existing = selectTrustedMarkerComment(comments, { marker: STATE_MARKER, label: 'Delivery V2 state', trustedLogin: trustedCommentAuthorForRepository(repository) });
`);
          replace('scripts/resume-delivery-v2-controller.mjs',
`  const actionsToken = requiredEnv('GITHUB_TOKEN');
`,
`  const actionsToken = requiredEnv('GITHUB_TOKEN');
  const controllerRunId = positiveInteger(requiredEnv('GITHUB_RUN_ID'), 'GITHUB_RUN_ID');
`);
          replace('scripts/resume-delivery-v2-controller.mjs',
`  const stateEnvelope = parseStateComment(await listComments(targetRepository, resumePr, targetReadToken));
  let state;
  let controller = stateEnvelope?.controller ?? {};
  if (stateEnvelope) {
`,
`  const trustedLogin = trustedCommentAuthorForRepository(targetRepository);
  const stateEnvelope = parseStateComment(await listComments(targetRepository, resumePr, targetReadToken), trustedLogin);
  if (!stateEnvelope) throw new Error('managed PR is missing authoritative Delivery V2 persistent state');
  const priorControllerRunId = positiveInteger(stateEnvelope.controller?.controllerRunId, 'persisted controllerRunId');
  const priorControllerRun = await api(\`https://api.github.com/repos/\${orchestratorRepository}/actions/runs/\${priorControllerRunId}\`, actionsToken);
  validateControllerRunProvenance(priorControllerRun, { orchestratorRepository, trustedRef: orchestratorRef });
  let state;
  let controller = { ...stateEnvelope.controller, controllerRunId, controllerRepository: orchestratorRepository, controllerRef: orchestratorRef, controllerWorkflowPath: '.github/workflows/delivery-v2-dispatch.yml' };
  if (stateEnvelope) {
`);
          replace('scripts/resume-delivery-v2-controller.mjs',
`      const releaseIdentity = releaseIdentityFromPullRequest(finalPullRequest, { materialHeadSha, baseSha: expectedBaseSha });
      const releaseInput = {
`,
`      const releaseIdentity = releaseIdentityFromPullRequest(finalPullRequest, { materialHeadSha, baseSha: expectedBaseSha });
      const mergePreview = await collectMergePreviewEvidence({ repository: targetRepository, pullRequest: finalPullRequest, materialHeadSha, baseSha: expectedBaseSha, workflowRun: latestSourceRun, requiredJobName: targetPolicy.mergePreviewJobName, token: targetReadToken });
      const releaseInput = {
`);
          replace('scripts/resume-delivery-v2-controller.mjs', `        mergePreview: releaseIdentity.mergePreview,`, `        mergePreview,`);

          // Target configuration declares the concrete merge-preview job whose workflow evidence the release gate consumes.
          replace('config/delivery-v2-controller-targets.json', `      "ciWorkflowPath": ".github/workflows/agent-check.yml",\n      "standardAuditRequired": true,`, `      "ciWorkflowPath": ".github/workflows/agent-check.yml",\n      "mergePreviewJobName": "Merge preview integration",\n      "standardAuditRequired": true,`);
          replace('config/delivery-v2-controller-targets.json', `      "ciWorkflowPath": ".github/workflows/validate-pr.yml",\n      "standardAuditRequired": true,`, `      "ciWorkflowPath": ".github/workflows/validate-pr.yml",\n      "mergePreviewJobName": "Merge preview compatibility",\n      "standardAuditRequired": true,`);
          replace('config/delivery-v2-controller-targets.json', `      "ciWorkflowPath": ".github/workflows/delivery-v2-ci.yml",\n      "standardAuditRequired": true,`, `      "ciWorkflowPath": ".github/workflows/delivery-v2-ci.yml",\n      "mergePreviewJobName": "V2 merge preview compatibility",\n      "standardAuditRequired": true,`);

          // This repository now validates merge-preview compatibility separately from exact-head full V2 checks.
          replace('.github/workflows/delivery-v2-ci.yml',
`jobs:
  foundation:
    name: V2 platform checks
`,
`jobs:
  merge_preview:
    name: V2 merge preview compatibility
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout merge preview
        uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci --no-audit --no-fund
      - name: Validate merge-preview contract compatibility
        run: npm run verify:v2

  foundation:
    name: V2 platform checks
    needs: merge_preview
`);
          replace('.github/workflows/delivery-v2-ci.yml',
`      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
`,
`      - name: Checkout exact PR head
        uses: actions/checkout@v4
        with:
          ref: \${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: Assert exact-head identity
        shell: bash
        env:
          EXPECTED_HEAD_SHA: \${{ github.event.pull_request.head.sha }}
        run: test "$(git rev-parse HEAD)" = "$EXPECTED_HEAD_SHA"

      - name: Setup Node.js
`);

          // Runtime capability gate: create-pull-request is disabled when target_pr selects remediation mode.
          for (const provider of ['claude', 'codex', 'copilot']) {
            for (const risk of ['fast', 'standard', 'critical']) {
              const path = `.github/workflows/delivery-v2-worker-${provider}-${risk}.md`;
              replace(path,
`permissions:
  contents: read
  issues: read
`,
`permissions:
  contents: read
  issues: read
env:
  GH_AW_POLICY_ALLOW_CREATE_PULL_REQUEST: "\${{ github.event.inputs.target_pr == '' && 'true' || 'false' }}"
`);
            }
          }

          // Regression controls.
          replace('test/v2-remediation-state-machine.test.mjs',
`  return { id, candidateSha: sha, blocksRelease: true, remediationMode: 'targeted', surface: 'src/example.mjs', failureMode: 'contract violation' };
`,
`  return { id, candidateSha: sha, severity: 'high', violatedContract: 'test contract', blocksRelease: true, remediationMode: 'targeted', surface: 'src/example.mjs', failureMode: 'contract violation', evidence: 'discriminating evidence' };
`);
          replace('test/v2-persistent-state.test.mjs',
`      candidateSha: SHA_A,
      surface: 'src/api.mjs',
      failureMode: 'missing guard',
      evidenceRef: 'github:finding/1'
`,
`      candidateSha: SHA_A,
      severity: 'critical',
      violatedContract: 'DV2-009',
      surface: 'src/api.mjs',
      failureMode: 'missing guard',
      evidence: 'guard is absent',
      remediationMode: 'systemic',
      blocksRelease: true,
      evidenceRef: 'github:finding/1'
`);
          replace('test/v2-controller-runtime.test.mjs',
`import { ciFailureClassForEvidence, expectedDispatchTitle, releaseIdentityFromPullRequest, selectCorrelatedWorkflowRun, validateAuditArtifactPayload } from '../src/v2/controller-runtime.mjs';
`,
`import { ciFailureClassForEvidence, expectedDispatchTitle, mergePreviewEvidenceFromWorkflow, releaseIdentityFromPullRequest, selectCorrelatedWorkflowRun, validateAuditArtifactPayload } from '../src/v2/controller-runtime.mjs';
`);
          replaceRegex('test/v2-controller-runtime.test.mjs', /test\('release identity fails closed on base drift and requires a mergeable preview',[\s\S]*?\n\}\);\n?$/, `test('release identity never fabricates merge-preview validation from GitHub mergeability', () => {
  const baseSha = 'c'.repeat(40);
  const previewSha = 'd'.repeat(40);
  const pr = { number: 64, html_url: 'https://github.com/owner/target/pull/64', head: { sha: SHA }, base: { sha: baseSha }, merge_commit_sha: previewSha, mergeable: true };
  const identity = releaseIdentityFromPullRequest(pr, { materialHeadSha: SHA, baseSha });
  assert.equal(identity.currentBaseSha, baseSha);
  assert.equal(identity.mergePreview.previewSha, previewSha);
  assert.equal(identity.mergePreview.status, 'pending');
  assert.equal(identity.mergePreview.conclusion, null);
  assert.throws(() => releaseIdentityFromPullRequest({ ...pr, base: { sha: 'e'.repeat(40) } }, { materialHeadSha: SHA, baseSha }), /base drift/);
});

test('merge-preview release evidence requires a successful named job from the exact PR/head/base workflow run', () => {
  const baseSha = 'c'.repeat(40);
  const previewSha = 'd'.repeat(40);
  const pr = { number: 64, html_url: 'https://github.com/owner/target/pull/64', head: { sha: SHA }, base: { sha: baseSha }, merge_commit_sha: previewSha };
  const run = { id: 10, event: 'pull_request', head_sha: SHA, html_url: 'run:10', pull_requests: [{ number: 64, head: { sha: SHA }, base: { sha: baseSha } }] };
  const evidence = mergePreviewEvidenceFromWorkflow({ pullRequest: pr, materialHeadSha: SHA, baseSha, workflowRun: run, jobs: [{ name: 'Merge preview compatibility', status: 'completed', conclusion: 'success', html_url: 'job:1' }], requiredJobName: 'Merge preview compatibility' });
  assert.equal(evidence.previewSha, previewSha);
  assert.equal(evidence.status, 'completed');
  assert.equal(evidence.conclusion, 'success');
  assert.equal(evidence.evidenceRef, 'job:1');
  const missing = mergePreviewEvidenceFromWorkflow({ pullRequest: pr, materialHeadSha: SHA, baseSha, workflowRun: run, jobs: [], requiredJobName: 'Merge preview compatibility' });
  assert.equal(missing.status, 'pending');
  assert.throws(() => mergePreviewEvidenceFromWorkflow({ pullRequest: pr, materialHeadSha: SHA, baseSha, workflowRun: { ...run, head_sha: 'e'.repeat(40) }, jobs: [], requiredJobName: 'Merge preview compatibility' }), /head mismatch/);
});
`);
          replace('test/v2-reentry-guard.test.mjs',
`import { operationalStateFromPersistent } from '../src/v2/operational-controller.mjs';
`,
`import { operationalStateFromPersistent } from '../src/v2/operational-controller.mjs';
import { validateControllerRunProvenance } from '../src/v2/controller-provenance.mjs';
`);
          replace('test/v2-reentry-guard.test.mjs',
`  const parsedState = parsePersistentStateEnvelope([{ id: 1, body: stateBody }]);
`,
`  const parsedState = parsePersistentStateEnvelope([{ id: 1, body: stateBody, user: { login: 'owner' }, author_association: 'OWNER' }], { trustedLogin: 'owner' });
`);
          replace('test/v2-reentry-guard.test.mjs',
`  assert.throws(() => parsePersistentStateEnvelope([{ id: 1, body: stateBody }, { id: 2, body: stateBody }]), /multiple Delivery V2 state comments/);
`,
`  assert.throws(() => parsePersistentStateEnvelope([{ id: 1, body: stateBody, user: { login: 'owner' }, author_association: 'OWNER' }, { id: 2, body: stateBody, user: { login: 'owner' }, author_association: 'OWNER' }], { trustedLogin: 'owner' }), /multiple Delivery V2 state comments/);
  assert.throws(() => parsePersistentStateEnvelope([{ id: 1, body: stateBody, user: { login: 'attacker' }, author_association: 'NONE' }], { trustedLogin: 'owner' }), /untrusted Delivery V2 state marker/);
`);
          replace('test/v2-reentry-guard.test.mjs',
`  const lease = parseBootstrapLease([{ id: 3, body: bootstrapBody }]);
`,
`  const lease = parseBootstrapLease([{ id: 3, body: bootstrapBody, user: { login: 'owner' }, author_association: 'OWNER' }], { trustedLogin: 'owner' });
`);
          write('test/v2-controller-provenance.test.mjs', `import assert from 'node:assert/strict';
import test from 'node:test';
import { parseTrustedJsonEnvelope, selectTrustedMarkerComment, validateControllerRunProvenance } from '../src/v2/controller-provenance.mjs';

const marker = '<!-- delivery-v2-state -->';
const body = marker + '\\n\\n\\`\\`\\`json\\n{"ok":true}\\n\\`\\`\\`';

test('controller state comments require trusted authorship before JSON becomes authoritative', () => {
  const trusted = { id: 1, body, user: { login: 'owner' }, author_association: 'OWNER' };
  assert.equal(selectTrustedMarkerComment([trusted], { marker, label: 'state', trustedLogin: 'owner' }).id, 1);
  assert.equal(parseTrustedJsonEnvelope([trusted], { marker, label: 'state', trustedLogin: 'owner' }).value.ok, true);
  assert.throws(() => parseTrustedJsonEnvelope([{ ...trusted, user: { login: 'attacker' }, author_association: 'NONE' }], { marker, label: 'state', trustedLogin: 'owner' }), /untrusted state marker/);
});

test('controller run provenance is bound to repository, dispatch workflow and trusted ref', () => {
  const run = { id: 11, event: 'workflow_dispatch', path: '.github/workflows/delivery-v2-dispatch.yml', head_branch: 'main', repository: { full_name: 'owner/orchestrator' } };
  assert.equal(validateControllerRunProvenance(run, { orchestratorRepository: 'owner/orchestrator', trustedRef: 'main' }).runId, 11);
  assert.throws(() => validateControllerRunProvenance({ ...run, head_branch: 'feature' }, { orchestratorRepository: 'owner/orchestrator', trustedRef: 'main' }), /trusted ref mismatch/);
  assert.throws(() => validateControllerRunProvenance({ ...run, path: '.github/workflows/other.yml' }, { orchestratorRepository: 'owner/orchestrator', trustedRef: 'main' }), /workflow path mismatch/);
});
`);
          replace('test/v2-worker-contract.test.mjs',
`      assert.match(body, /create-pull-request:/);
`,
`      assert.match(body, /GH_AW_POLICY_ALLOW_CREATE_PULL_REQUEST: "\\$\\{\\{ github\\.event\\.inputs\\.target_pr == '' && 'true' \\|\\| 'false' \\}\\}"/);
      assert.match(body, /create-pull-request:/);
`);
          replace('test/v2-worker-contract.test.mjs',
`      assert.doesNotMatch(body, /merge-pull-request:/);
`,
`      assert.match(lockBody, /GH_AW_POLICY_ALLOW_CREATE_PULL_REQUEST/);
      assert.doesNotMatch(body, /merge-pull-request:/);
`);

          // Lossless remediation round-trip regression.
          replace('test/v2-operational-controller.test.mjs',
`  persistentStateFromOperational
} from '../src/v2/operational-controller.mjs';
`,
`  persistentStateFromOperational,
  operationalStateFromPersistent
} from '../src/v2/operational-controller.mjs';
`);
          fs.appendFileSync('test/v2-operational-controller.test.mjs', `

test('persistent resume preserves actionable CI and semantic audit remediation packets without degradation', () => {
  let ciState = createOperationalDelivery({ plan: planFor('critical'), materialHeadSha: A });
  ciState = applyOperationalEvent(ciState, { type: 'ci-result', result: { candidateSha: A, conclusion: 'failure', failureClass: 'actionable', cause: 'unit assertion failed', evidenceRef: 'run:ci-fail' } });
  const ciPersistent = persistentStateFromOperational({ state: ciState, identity: identity(), classifier: { version: 'v1', fingerprint: 'fingerprint' } });
  const ciRestored = operationalStateFromPersistent(ciPersistent);
  assert.deepEqual(operationalRemediationInput(ciRestored).ciFailure, ciState.ciFailure);

  let auditState = createOperationalDelivery({ plan: planFor('critical'), materialHeadSha: A });
  auditState = applyOperationalEvent(auditState, { type: 'ci-result', result: { candidateSha: A, conclusion: 'success', evidenceRef: 'run:ok' } });
  auditState = applyOperationalEvent(auditState, { type: 'audit-result', result: { candidateSha: A, decision: 'rejected', evidenceRef: 'audit:reject', findings: [{ id: 'DV2-TEST-ROUNDTRIP', candidateSha: A, severity: 'critical', violatedContract: 'DV2-009', blocksRelease: true, remediationMode: 'systemic', surface: 'src/v2/example.mjs', failureMode: 'resume loses evidence', evidence: 'round-trip mismatch' }] } });
  const auditPersistent = persistentStateFromOperational({ state: auditState, identity: identity(), classifier: { version: 'v1', fingerprint: 'fingerprint' } });
  const auditRestored = operationalStateFromPersistent(auditPersistent);
  assert.deepEqual(operationalRemediationInput(auditRestored).findings, auditState.blockingFindings);
  assert.deepEqual(auditRestored.auditEvidence, auditState.auditEvidence);
});
`);
          NODE

      - name: Install pinned gh-aw
        uses: github/gh-aw/actions/setup-cli@bde367913adeb3132f0a171594c88a17f4b7d08c
        with:
          version: v0.88.7
      - name: Compile workers
        run: gh aw compile --strict
      - name: Focused and full V2 validation
        run: |
          node --check src/v2/controller-provenance.mjs
          node --check src/v2/controller-runtime.mjs
          node --check src/v2/persistent-state.mjs
          node --check src/v2/operational-controller.mjs
          node --check scripts/guard-delivery-v2-reentry.mjs
          node --check scripts/reserve-delivery-v2-initial-attempt.mjs
          node --check scripts/run-delivery-v2-controller.mjs
          node --check scripts/resume-delivery-v2-controller.mjs
          node --test test/v2-controller-provenance.test.mjs test/v2-controller-runtime.test.mjs test/v2-persistent-state.test.mjs test/v2-operational-controller.test.mjs test/v2-reentry-guard.test.mjs test/v2-remediation-state-machine.test.mjs test/v2-worker-contract.test.mjs
          npm run verify:v2
          npm run verify:v2:complete
      - name: Publish clean functional candidate
        shell: bash
        run: |
          git rm .github/workflows/delivery-v2-r3-remediation.yml
          git config user.name "delivery-v2-remediation"
          git config user.email "delivery-v2-remediation@users.noreply.github.com"
          git add src/v2 scripts test config .github/workflows .github/aw
          git commit -m "fix(v2): close systemic release and resume audit gaps"
          git push origin HEAD:feat/63-v2-operational-efficiency
